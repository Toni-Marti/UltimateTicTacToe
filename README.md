To run the aplication:

`cd back_end`

`npm install`

`echo "{"users":[]}" >> db.json`

`npx json-server ./db.json --port 9999 &`

`npm start &`

`cd ../front_end`

`npm install`

`npm start &`

Only do the installs first time.